{
  "name": "threads-notion-sync",
  "version": "2.0.1",
  "description": "MOONGI Studio — Threads to Notion engagement tracker",
  "type": "module",
  "main": "threads-sync.js",
  "scripts": {
    "sync": "node threads-sync.js --run-now",
    "schedule": "node threads-sync.js",
    "logs": "tail -f sync.log",
    "full-sync": "node threads-sync.js --full-sync",
    "archive": "node archive-local.js",
    "archive:force": "node archive-local.js --force"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0",
    "libsodium-wrappers": "0.7.13",
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2"
  }
}
